# Particle_swarm_optimization
  - 作为一篇1995年发表的关于PSO算法的相关定义、猜想与尝试，其核心思想是**引入了pbest（全局最优）和gbest（个人历史最优）**，其所提出的：
    **与 pbest相关的速度调整被称为“简单怀旧”，因为个体倾向于回到过去最令它满意的地方。另一方面，gbest 在概念上类似于公开的知识，或者说是一个群体规范或标准，个体试图去达到这个标准**  的观点真是具有哲学意义与技术意义两层较为深刻的含义——利用全局当下最优和个体历史最优对每个“智能体”（即存在的每个解）的更新迭代进行优化指导。  
  - 相关公式如下：  
  （1）PSO 的个体速度更新公式为：v_i(t+1) = w * v_i(t) + c1 * r1 * (pbest_i - x_i(t)) + c2 * r2 * (gbest - x_i(t))（这里的 w 即惯性因子，即保持原有速度的比例；后面所提到的“自适应PSO”也就是该 w 会随着迭代而不断改变）  
  （2）位置更新公式为：x_i(t+1) = x_i(t) + v_i(t+1)（即新位置就是*老位置加上新速度*）

# PSO + BP  
- gbest被替换为“全局历史最优”，也就是“截止到目前所有出现过的粒子的最优”
- 文中明确提到了超参数的优化对于我们的该任务的重要性，故我们需要对 w 以及 w 的选择策略进行详细设计，另外，还有PSO选取的编码方式（矩阵？向量？还是0/1？）也值得考虑
- 另一个需要明确的重点是，该文章使用的是PSO进行**网络训练**，与我们的**网络初始化**有着很大的差别。文章中对于适应值函数的设计就是简单粗暴地将其设置为“E”（即每轮训练的传播误差），但是我们作为“初始化目的”，则也需要对适应值函数进行设计。
- 另外文中的实验验证设计也值得我们效仿：对early-stage的损失率的折线图可视化，以及耗时对比；对鸢尾花数据集的acc比较，以及消耗CPU资源对比。

# 思考
首先是明确“PSO训练策略”与“PSO初始化策略”的异同，二者之间最大的区别或许就是适应值函数，我们可以围绕该点进行一定的创新。或许二者之间的壁垒也不是那么清晰。
其次，超参数的选取与优化一定要慎重，需要进行调参。且 w 的自适应选取策略也需要设计。

  
