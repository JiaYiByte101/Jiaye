# SuperPoint: Self-Supervised Interest Point Detection and Description
## 核心思想
创建一个共同编码器，然后分出“KeyPoint解码器”和“Descriptor解码器”分别输出一个全像素的heatmap以及像素级一一对应的descriptor

## 具体操作
### 编码阶段
作为共同编码器，编码阶段采取两个卷积层，以及三个最大池化层，将原始图像尺寸的(H,W,F)变为(H/8,W/8,F)，其中每个"CELL"代表原图8×8的一个区块。

### KeyPoint解码器  
由于现在编码器输入的为(H/8,W/8)的下采样后的图像，故解码器需要想办法将其重新上采样到原始尺寸，具体操作为：
- 首先使用1×1卷积核对 (H/8,W/8)进行卷积操作，得到(H/8,W/8,65)（第65个通道是对这个8×8区域的定性，如果整个区域都没有KeyPoint，就设置为1，所以又叫做“垃圾通道”）
- 然后对该(H/8,W/8,65)进行上采样，具体操作为直接reshape为(H/8,W/8)的原始图像尺寸，64个通道从左上到右下依次排开，还原为原始尺寸的每个像素对应的输出值，然后进行softmax
- PS：这里之所以1×1卷积能够实现该“左上到右下按序排列”的效果，是监督信号给他的相关提示：“监督信号指出I（1,5）是KeyPoint”这种类似的形式，会使得卷积网络学习到对应的提取模式，再加上Reshape的通道与空间绑定的机制，可以实现还原为原始尺寸的每个像素对应的输出值的效果。

### Descriptor解码器
也是一样，由于现在编码器输入的为(H/8,W/8)的下采样后的图像，故解码器需要想办法将其重新上采样到原始尺寸，具体操作为：
- 首先对(H/8,W/8)进行卷积操作，提取每个CELL的descriptor，形成(H/8,W/8,D)，其中D就是descriptor的维度
- 而后也要进行上采样。这里采取的操作是双三次插值 bicubic interpolation，在(H/8,W/8,D)尺寸下，计算每个原始尺寸在该图像中的位置（例如（8，8）计算对应位置就是（1，1）），而后在该位置取周围的4×4区域的descriptor进行加权平均插值
- 最后要对上采样后的原始图像尺寸的(H,W,D)进行L2 Normalization，以对后面求两个像素的descriptr的余弦相似度来判断是否匹配做铺垫
  PS：这里推理所用的才是上采样后的(H,W,D)，而训练所用的依旧是(H/8,W/8,D)，因为本身descriptor所包含的语义信息就是稀疏的，没必要使用(H,W,D)平白无故增加计算复杂度。

### Loss Funtion
由两部分组成（事实上是三项）：KeyPoint部分 和 Descriptor部分
- KeyPoint部分 有两项组成：（1）原始图像的交叉熵 （2）单应变换之后的交叉熵。
- 其中每个图像的交叉熵计算过程即将每个Cell提取出的logits与标签的独热编码（因为一个Cell只能提取一个KeyPoint）进行交叉熵损失计算
- Descriptor部分为将每个Cell的中心点（这里就对应我们上面提到的训练所用的依旧是(H/8,W/8,D)，每个descriptor事实上都是与一个Cell相对应的）
- 将原图中每个 descriptor 所在 cell 的中心点做单应变换，找到变换图中最邻近的 descriptor；若距离 ≤ 8 px 则判为正样本，否则为负样本。
- 然后对一对正/负样本做余弦相似度度量，之后带入Hinge Loss进行计算

### MagicPoint 和 Homographic Adaptation
- 二者为基础与延申的关系，Homographic Adaptation是本文非常重要的一个创新，同时也是性能提升的非常关键的一个模块
- MagicPoint为在合成几何图形数据集上训练的KeyPoint检测器，但是其在现实生活中的非角形场景中表现并不好，故需要Homographic Adaptation对其进行修正
- Homographic Adaptation核心思想为：当一个图像进行了单应变换后，其变换后对应的KeyPoint进行对应的反变换能够对应上变换前的位置。
- 那么Homographic Adaptation就利用对图像进行Nh次单应变换生成Nh张视图，采用MagicPoint进行关键点检测，而后将这Nh张视图反变换回去，再将这些被反变换的关键点做聚合操作（通常是平均），进而为一张图片做了“伪标签”的生成
- Homographic Adaptation 实现了从“合成数据训练 → 真实图像迁移”的关键跳板，是自监督思想的体现。


# SuperGlue
# 核心思想
SuperPoint 提取出的 KeyPoint 和 Descriptor 尽管鲁棒性较强，但后续匹配仍采用基于欧氏距离的启发式策略（如最近邻 + 双向检查），难以建模上下文语义，遇到遮挡、重复结构、光照变化等挑战时，容易导致错误匹配或漏匹配。SuperGlue 则将匹配过程纳入神经网络框架，改为端到端可学习的“上下文感知匹配”

# 具体操作
## SuperPoint提取关键点与描述子
依旧是使用SuperPoint对关键点与描述子进行提取。将两副需要匹配的图像送入SuperPoint，得到关键点与描述子矩阵，得到关键点的位置 (xi,yi)与描述子向量di后，将两者拼接为节点特征（descriptor embedding + positional encoding），作为图神经网络的输入。

## 图神经网络
图神经网络的每个节点都是关键点的位置信息、描述子的语义信息的综合。接下来需要进行两步操作：
1.图内注意力机制。将图内的所有节点进行自注意力操作迭代更新，得到结合了本图中其他节点上下文语意信息的新节点。
2.图间注意力机制。将两个图之间的节点进行自注意力操作更新，得到了结合了另一张图上下文语义信息的新节点。操作与图内没有差别，只是节点换成了另一张图的相关节点。
以上操作为一层的操作，重复L层（一般为9层）加深语义传播，然后输出两张图每个KeyPoint的最终表示F1i与F2j.
至此，两张图的相关节点都已经掌握了“全图上下文 + 跨图注意信息”的相关信息，这对于后面的匹配与容错有着很大的影响。

## Sinkhorn 迭代正规化
通过 GNN 得到图像 A 和 B 中所有关键点的最终特征表示F1i与F2j，现在我们需要推导得出对应节点之间的匹配概率矩阵S：
- SuperGlue首先计算两组描述子之间的匹配得分：M<f1i,f2j>，通过做点积或者其他相关度量相似性的算法，得到该概率。
- 而后需要加入Dummy行列，以计算KeyPoint“没有在另一张图中找到相似点”的概率
Sinkhorn 正规化：
- 现在我们希望将打分矩阵 M 转换为一个“双随机矩阵”，即每个元素都在0到1之间，且每行每列的和都为1。
- 需要在行维度、列维度上交替进行 softmax，最终收敛到一个“正规化的 soft-permutation 矩阵”
至此，我们便得到了一个两张图之间对应所有节点的匹配概率矩阵S，那么就可以直接进行最后结果的输出了，不管是硬匹配（直接输出最大概率的作为匹配点）还是软匹配（设置阈值、Top-k筛选等），就可以得到对应的输出

## Loss Function
这整个训练机制中，损失函数的计算值得仔细设计，因为我们无法将其直接应用于某个固定的损失函数，因为其标签-预测值之间的关系并不是简单的输出做差的关系：
- 这里采用利用某个数据集SfM进行三维重建，而后将对应的两个KeyPoint映射到三维模型中查看其是否是“匹配点”进行监督
- 所用的损失函数模板为交叉熵损失，具体公式为：L = -ΣΣGijlog(Sij)
- - Gij即对应的两个KeyPoint在三维模型中是否是匹配点的关系，是为1，不是为0。
  - 该损失函数便能够惩罚“对应匹配点在匹配概率模型中值却很小”的错误，进而引导模型向正确的方向进行学习。


# OmniGLue
## 核心思想
现存的基于学习的图像匹配算法，大部分都是基于特定数据集进行训练，这就导致其域的僵化，泛化能力很差，在某些新型的图像上（如航拍数据等等）精度很低；
而传统手工提取特征的相关算法如SIFT，虽然依赖于纯手工的算法步骤使得其泛化能力比较强，不依赖于特定的域，但其性能却比较差，对于某些光照等等因素影响较大的图像无法匹配
在此基础上，本文的主要目标导向是高泛化能力，基于传统的“寻找、提取、匹配”的管线，在SuperGlue的基础上（文中提到了SuperGlue是与OmniGlue最相似的模型，我认为确实是在SuperGlue的基础上进行的改进），引入了大模型的泛化知识能力来引导模型特征匹配，并改进注意力机制以解耦空间信息对匹配的影响。

## Parse 1
使用两种互补的编码器对图像进行编码，提取图像特征：
- SuperPoint 具有良好稳定的KeyPoint和Descriptor的提取性能，细粒度
- DINOv2 作为ViT大模型的衍生，具有良好的泛化知识能力，粗粒度
首先模型使用SuperPoint提取KeyPoint和Descriptor进行，记作p与d，作为关键点的两个特征。
其次模型使用DINOv2对两个图像进行稠密特征图的生成，而后映射到SuperPoint的图像维度中，使用插值得到g（DINOv2提取的特征），作为关键点的第三特征

## Parse 2
图神经网络的生成。与SuperGlue一样，OmniGlue也是一样采取的图神经网络，不过不同的是它不仅生成了图像内，还生成了图像间图神经网络，并且这里的图像间神经网络还是有向图，也就是一共生成了四幅图。
而两幅图像间神经网络则需要引入第三特征g进行剪枝：
- 对是否要在两个节点之间建立连接，首先将这两个节点的g进行点积求相似度，然后剪枝相似度后50%的连接

## Parse 3
对于图内和图间，与SuperGlue一样执行注意力机制，不同的是，Q和K都引入了d+p（即描述子加位置编码），但V则只有描述子d，为的是使得模型输出不再侧重于参考位置信息，而是侧重于纹理花纹进行匹配。

## Parse 4
进行注意力的更新后，每个节点的d便都已经结合图内和图间的所有节点的上下文信息，这样一来，在进行相似性度量（如点积），便可构造出与SuperGlue一样的匹配相似矩阵S
再执行Sinkhorn迭代正规化，将矩阵的行列值相加都为1。
后续即可执行软硬匹配

  


